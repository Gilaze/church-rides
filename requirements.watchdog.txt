# Minimal requirements for running the watchdog service only
# Install with: pip install -r requirements.watchdog.txt

requests>=2.31.0
psycopg2-binary>=2.9.9

# Optional: if using .env file for configuration
# python-dotenv>=1.0.0
